#![allow(dead_code)]

// Application-owned extension methods for `locale_settings`.
// This file is created by rozectl but preserved during `--update`.
use super::locale_setting::{LocaleSettingRepository, Model};

impl Model {
    // Add domain helpers for generated SeaORM model rows here.
}

impl<'a> LocaleSettingRepository<'a> {
    // Add application-owned repository queries here.
}
